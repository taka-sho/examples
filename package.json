{
  "name": "examples",
  "private": true,
  "scripts": {
    "build": "gulp build",
    "lint:pug": "gulp lint",
    "lint:ts": "tslint gulpfile.ts gulp/**/*.ts",
    "lint": "npm run lint:pug && npm run lint:ts",
    "postinstall": "typings install",
    "prebuild": "rm -rf dist",
    "pretest": "npm run lint",
    "start": "gulp serve",
    "test": "npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@uncovertruth/tslint-config": "git://github.com/uncovertruth/tslint-config.git",
    "gulp": "^3.9.1",
    "gulp-connect": "^4.1.0",
    "gulp-if": "^2.0.1",
    "gulp-pug": "^3.0.3",
    "gulp-pug-lint": "^0.1.6",
    "pug": "^2.0.0-beta3",
    "pug-lint-config-clock": "^2.0.0",
    "require-dir": "^0.3.0",
    "tslint": "^3.12.1",
    "typescript-require": "^0.2.9-1",
    "typings": "^1.3.1"
  }
}
