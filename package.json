{
  "name": "examples",
  "private": true,
  "scripts": {
    "background": "selenium-standalone start",
    "build": "gulp build",
    "postinstall": "typings install && selenium-standalone install",
    "prebuild": "rm -rf dist",
    "test": "npm run build"
  },
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-connect": "^4.1.0",
    "gulp-if": "^2.0.1",
    "gulp-pug": "^3.0.3",
    "pug": "^2.0.0-beta3",
    "require-dir": "^0.3.0",
    "selenium-standalone": "^5.1.1",
    "tslint": "^3.11.0",
    "typescript-require": "^0.2.9-1",
    "typings": "^1.1.0"
  }
}
