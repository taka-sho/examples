machine:
  timezone: UTC
  node:
    version: 6.1.0

general:
  artifacts:
    - "dist"

dependencies:
  post:
    - npm start:
        background: true

deployment:
  master:
    branch: master
    commands:
      - sh ./deploy.sh
